import { OptionResolver } from '../relationship.resolver';

describe("OptionResolver", () => {
  it("signIn should return redirect url with code", () => {
    const resolver: OptionResolver = new OptionResolver()
  })
})