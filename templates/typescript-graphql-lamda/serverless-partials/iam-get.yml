Type: AWS::IAM::Role
Properties:
  Path: /
  RoleName: "${self:service}-getDispatchEstimates-${self:custom.environment}"
  AssumeRolePolicyDocument:
    Version: "2012-10-17"
    Statement:
      - Effect: Allow
        Principal:
          Service: lambda.amazonaws.com
        Action: sts:AssumeRole
  Policies:
    - PolicyName: LambdaDefaultPolicy
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Action:
              - logs:CreateLogGroup
              - logs:CreateLogStream
              - logs:PutLogEvents
            Resource:
              - "arn:aws:logs:${self:custom.region}:${self:custom.account}:log-group:/aws/lambda/${self:service}-getDispatchEstimates-${self:custom.environment}*"
    - PolicyName: LambdaReadDynamoDbPolicy
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Action:
              - dynamodb:GetItem
              - dynamodb:Query
              - dynamodb:Scan
            Resource:
              - "arn:aws:dynamodb:${self:custom.region}:${self:custom.account}:table/${self:service}-${self:custom.environment}-v2"
              - "arn:aws:dynamodb:${self:custom.region}:${self:custom.account}:table/${self:service}-${self:custom.environment}-v2/index/StackLocaleSlug"